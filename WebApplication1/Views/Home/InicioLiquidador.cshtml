@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    @Scripts.Render("~/bundles/bootstrap")
}

    <div class="col-md-4 col-md-offset-3 text-center" id="agregaroperador"><p><h2>Bienvenido</h2></div>
    <div class="col-md-4 col-md-offset-3 text-center" id="agregaroperador"><p>Haz click sobre el nombre de algun operador para acceder al registro de horas trabajadas del mes actual</p><p>Si es la primera vez que accedes, haz click en el simbolo + para agregar un operador.</p></div>
    <div class="col-md-4 col-md-offset-3 text-center desplegar">
        @foreach (WebApplication16.Models.Operadores operadores in ViewBag.Operadores)
        {
            <form action="/Home/ConsultarRegLiq" method="post" id="@operadores.DNI">
                <input type="text" value="@operadores.DNI" name="operador" hidden />
                <a href="#" onclick="convertir()" id="@operadores.DNI"><span class="glyphicon glyphicon-minus" id="tamaño"></span>@operadores.Nombres @operadores.Apellidos</a>
            </form>
        }
        <div class="row" id="error" hidden>@TempData["Error3"]</div>
    </div>
